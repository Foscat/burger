<li>
	{{burger_name}}

	<button class="change-eat" data-id="{{id}}" data-newbuger="{{devoured}}">
		{{#if devoured}}ALL GONE BUSTER!{{else}}EAT UP!{{/if}}
	</button>

	<button class="devour-burger" data-id="{{this.id}}">DEVOUR!</button>
</li>