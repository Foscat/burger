<li>
	{{burger_name}}

	<button class="change-eat" data-id="{{this.id}}" data-neweaten="{{eaten}}">
		{{#if eaten}}ALL GONE BUSTER!{{else}}EAT UP!{{/if}}
	</button>

	<button class="devour-burger" data-id="{{this.id}}">DEVOUR!</button>
</li>